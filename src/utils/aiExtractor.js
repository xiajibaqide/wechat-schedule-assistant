export async function extractEventWithAI(messageText) {
  // Future AI extraction should be called through a backend or secure proxy.
  // Do not place API keys or direct paid API calls in this frontend file.
  return null;
}
